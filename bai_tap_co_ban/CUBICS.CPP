#include <bits/stdc++.h>

using namespace std;

int main(){
    freopen("cubics.inp", "r", stdin);
    freopen("CUBICS.OUT", "w", stdout);
    long long n; cin >> n;
    cout << n * (n + 1) / 2 << endl;
    return 0;
}