#include <bits/stdc++.h>

using namespace std;

int main(){
    freopen("NOIQUY.INP", "r", stdin);
    freopen("NOIQUY.OUT", "w", stdout);
    int a, b, c, t, m, n; cin >> a >> b >> c >> t >> m >> n;
    cout << a * t + b * m + c * n << endl;
    return 0;
}