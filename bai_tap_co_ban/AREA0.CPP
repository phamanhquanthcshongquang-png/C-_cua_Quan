#include <bits/stdc++.h>

using namespace std;

int main(){
    freopen("AREAP.INP", "r", stdin);
    freopen("AREA0.OUT", "w", stdout);
    float a, b; cin >> a >> b;
    cout << a * b - a * a * 3.14 / 4 << endl;
}